[package]
name = "anthropic-text-editor"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.27", features = ["derive"] }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

thiserror = "2.0"
walkdir = "2.5.0"

[dev-dependencies]
tempfile = "3.15.0"
